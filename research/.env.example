# Greenplum (copy from D:\SQL скрипты\документация\password.json)
GP_HOST=your_greenplum_host
GP_PORT=5432
GP_DATABASE=your_database
GP_USER=your_user
GP_PASSWORD=your_password

# Firebase Admin (для publish.py — пока не нужен в R1)
# FIREBASE_PROJECT_ID=impact-calc-lp
# FIREBASE_ADMIN_CREDENTIALS_JSON=/path/to/serviceAccount.json
